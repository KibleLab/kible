import AppBar from '../components/AppBar';
import NavBar from '../components/NavBar';

const OrderSheet = () => {
  return (
    <div>
      <AppBar name={'Menu'} />
      <NavBar />
    </div>
  );
};

export default OrderSheet;
