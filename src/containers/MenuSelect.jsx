import AppBar from '../components/AppBar';
import NavBar from '../components/NavBar';

const MenuSelect = () => {
  return (
    <div>
      <AppBar name={'Menu'} />
      <NavBar />
    </div>
  );
};

export default MenuSelect;
