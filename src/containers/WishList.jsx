import AppBar from '../components/AppBar';
import NavBar from '../components/NavBar';

const WishList = () => {
  return (
    <div>
      <AppBar name={'찜목록'} />
      <NavBar />
    </div>
  );
};

export default WishList;
